"""RL / particle-filter infrastructure built on top of the Set Transformer core.

Importing anything in this subpackage requires the optional ``rl`` extras
(``gymnasium``, ``stable-baselines3``, ``filterpy``). The core
``set_transformer`` package can be used standalone without installing these.
"""
