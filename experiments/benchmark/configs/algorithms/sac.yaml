# SAC
# stable-baselines3 SAC; unlisted values are SB3 defaults.
# GENERATED by experiments/benchmark/generate_configs.py -- do not edit by
# hand; edit the registry or train.py and regenerate.

algorithm: SAC
learning_rate: 0.0003
batch_size: 64
n_envs: 4
gamma: "per-env (see envs/<env>.yaml)"
device: auto
requires_continuous_actions: true
note: "SAC leaves policy.features_extractor as None and builds separate actor/critic extractors; train.py::find_features_extractor handles that"
