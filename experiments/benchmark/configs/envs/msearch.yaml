# Environment: msearch
# EnvSpec as registered.
# GENERATED by experiments/benchmark/generate_configs.py -- do not edit by
# hand; edit the registry or train.py and regenerate.

env: msearch
builder: set_transformer.rl.benchmark.envs.make_msearch_base_env
base_env_kwargs: {}
spaces:
  obs_dim: 67
  num_particles: 100
  particle_dim: 2
  action_space: Box(-1.0, 1.0, (2,), float32)
belief:
  particle_filter: MultimodalSearchParticleFilter
  num_particles: 100
  particle_filter_kwargs:
    k_max: 10
    arena_half: 14
    visibility_radius: 1.5
  pf_kwargs_from_env: false
  pf_mapper: null
  obs_mask_indices: 7..66
  particles_relative_to_agent: true
reward:
  shaping_potential: pf_belief_information_potential(scale=100.0)
  shaping_gamma: 1
  shaping_zero_at_termination: false
  success_criterion: "found the target before the horizon (return > 0)"
training:
  default_algo: SAC
  default_timesteps: 600000
  gamma: 0.99
  max_ep_steps: 42
  particle_scale: 14
