# Environment: odd_even
# EnvSpec as registered.
# GENERATED by experiments/benchmark/generate_configs.py -- do not edit by
# hand; edit the registry or train.py and regenerate.

env: odd_even
builder: set_transformer.rl.benchmark.envs.make_odd_even_base_env
base_env_kwargs: {}
spaces:
  obs_dim: 1
  num_particles: 100
  particle_dim: 1
  action_space: Discrete(10)
belief:
  particle_filter: ParityAwareOddEvenParticleFilter
  num_particles: 100
  particle_filter_kwargs:
    n_dist_size: 10
    std_dev: 2
  pf_kwargs_from_env: false
  pf_mapper: null
  obs_mask_indices: null
  particles_relative_to_agent: false
reward:
  shaping_potential: null
  shaping_gamma: null
  shaping_zero_at_termination: true
  success_criterion: "mean per-step reward > -2.0, i.e. under ~2.5% of steps spent on a wrong-parity (impossible) state"
training:
  default_algo: PPO
  default_timesteps: 500000
  gamma: 0.99
  max_ep_steps: 100
  particle_scale: 10
