# Environment: ant_tag
# EnvSpec as registered.
# GENERATED by experiments/benchmark/generate_configs.py -- do not edit by
# hand; edit the registry or train.py and regenerate.

env: ant_tag
builder: set_transformer.rl.benchmark.envs.make_ant_tag_base_env
base_env_kwargs: {}
spaces:
  obs_dim: 31
  num_particles: 100
  particle_dim: 2
  action_space: Box(-1.0, 1.0, (8,), float32)
belief:
  particle_filter: AntTagParticleFilter
  num_particles: 100
  particle_filter_kwargs: {}
  pf_kwargs_from_env: true
  pf_mapper: ant_tag_pf_interaction_mapper
  obs_mask_indices: -2..-1
  particles_relative_to_agent: false
reward:
  shaping_potential: ant_tag_true_state_potential
  shaping_gamma: null
  shaping_zero_at_termination: true
  success_criterion: episode_length < max_ep_steps (target tagged before timeout)
training:
  default_algo: PPO
  default_timesteps: 2000000
  gamma: 0.99
  max_ep_steps: 400
  particle_scale: 4.5
